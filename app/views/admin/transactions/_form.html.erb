<%= form_for(@admin_transaction) do |f| %>
  <% if @admin_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_transaction.errors.count, "error") %> prohibited this admin_transaction from being saved:</h2>

      <ul>
      <% @admin_transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :params %><br>
    <%= f.text_field :params %>
  </div>
  <div class="field">
    <%= f.label :trade_number %><br>
    <%= f.text_field :trade_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
